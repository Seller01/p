import {Ajax} from "../utility/Ajax";

const api = new Ajax({
    baseUrl: undefined,
    addAntiforgeryHeader: true,
});

export default {
    api,
    install(Vue, options) {
        Vue.prototype.$api = api;
    }
}